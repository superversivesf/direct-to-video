services:
  pitchstorm:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - pitchstorm-data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/pitchstorm.db
volumes:
  pitchstorm-data: